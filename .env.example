## Backend configuration
PORT=5000

## MongoDB connection (required)
## Local MongoDB:
MONGODB_URI=mongodb://127.0.0.1:27017/login-app

## CORS allowed origins (comma-separated). Optional for local dev.
## If not set, the server reflects the request Origin.
# CLIENT_ORIGIN=http://localhost:5000

